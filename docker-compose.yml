version: "2"

services:
  app:
    environment: 
    - NODE_ENV=production
    container_name: app
    build: ./app
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    links:
      - mongo
  mongo:
    image: mongo:3.4.1
    ports:
      - "27017:27017"