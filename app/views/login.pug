extends admin-layout

block content
  section
    div.container
      form(method="POST")
        label(for="username")
        input#username(type="text", name="username",placeholder="Usuario")
        label(for="password")
        input#username(type="text", name="password",placeholder="Contraseña")
        button(type="submit") Ingresar!