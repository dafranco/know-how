extends admin-layout

block content
	section
		form(method="POST")
			label(for="readable_name")
			input#username(type="text", name="readable_name",placeholder="Nombre y Apellido")
			label(for="username")
			input#username(type="text", name="username",placeholder="Nombre de Usuario")
			label(for="password")
			input#username(type="password", name="password",placeholder="Contraseña")
			button(type="submit") Registrarse!